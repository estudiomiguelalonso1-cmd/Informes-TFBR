# Códigos de cuenta repetidos — para resolver con contaduría

Generado automáticamente por la app (`duplicados_tfbr.js`) a partir de los archivos
de julio 2026. Cada caso es un código que aparece dos veces en la hoja `SALDOS` de un
balance. **Ninguno se puede resolver sin una decisión contable**, por eso la app los
avisa en cada corrida pero no los toca.

Mientras no se resuelvan, casi todas estas cuentas están hoy en cero, así que no
distorsionan el balance de julio — pero el problema se activa solo el mes que alguna
tenga movimiento.

## Balance Mensual $ — 1 caso(s)

### 4212800000 — Las dos filas son idénticas: el importe se cuenta dos veces

- **Fila 174**: `4212800000  TASAS AFIP` — la referencian: nadie
- **Fila 233**: `4212800000  TASAS AFIP` — la referencian: `Anexo II!D109`, `Anexo II!F110`

**Qué hay que decidir:** Las dos filas tienen el texto idéntico, así que las dos levantan el mismo importe y se cuenta dos veces. Cuál de las dos sobra es una decisión contable.

## Balance Mensual R$ — 5 caso(s)

### 2110102400 — Una de las dos nunca levanta su importe

- **Fila 53**: `2110102400  OSDE` — la referencian: nadie
- **Fila 57**: `2110102400   OSDE` — la referencian: nadie

**Qué hay que decidir:** Ninguna de las dos matchea contra la zona de pegado en esta corrida, así que no se puede saber cuál queda viva. Se revisa a mano.

### 2110803600 — Las dos filas son idénticas: el importe se cuenta dos veces

- **Fila 78**: `2110803600  OPUS INSPECTION S.A.` — la referencian: nadie
- **Fila 100**: `2110803600  OPUS INSPECTION S.A.` — la referencian: nadie

**Qué hay que decidir:** Las dos filas tienen el texto idéntico, así que las dos levantan el mismo importe y se cuenta dos veces. Cuál de las dos sobra es una decisión contable.

### 2113200000 — Las dos filas son idénticas: el importe se cuenta dos veces

- **Fila 95**: `2113200000  SERGIO MARCELO MARTIN` — la referencian: nadie
- **Fila 109**: `2113200000  SERGIO MARCELO MARTIN` — la referencian: nadie

**Qué hay que decidir:** Las dos filas tienen el texto idéntico, así que las dos levantan el mismo importe y se cuenta dos veces. Cuál de las dos sobra es una decisión contable.

### 4223600000 — El mismo código lo usan dos cuentas diferentes

- **Fila 207**: `4223600000  ADELANTO VIAJE` — la referencian: `Anexo II!E10`
- **Fila 208**: `4223600000  DEUDORES INCOBRABLES` — la referencian: `EERR!C21`, `Anexo II!E97`

**Qué hay que decidir:** El código lo usan dos cuentas diferentes ("4223600000  ADELANTO VIAJE" y "4223600000  DEUDORES INCOBRABLES"). Borrar cualquiera elimina una cuenta real del balance.

### 4212800000 — Las dos filas son idénticas: el importe se cuenta dos veces

- **Fila 166**: `4212800000  TASAS AFIP` — la referencian: `Anexo II!D105`
- **Fila 222**: `4212800000  TASAS AFIP` — la referencian: `Anexo II!E21`

**Qué hay que decidir:** Las dos filas tienen el texto idéntico, así que las dos levantan el mismo importe y se cuenta dos veces (además cada una alimenta líneas distintas: Anexo II!D105 y Anexo II!E21). Cuál de las dos sobra es una decisión contable.

## Balance Acumulado $ — 7 caso(s)

### 4120500000 — Las dos filas son idénticas: el importe se cuenta dos veces

- **Fila 116**: `4120500000  INTERESES GANADOS` — la referencian: `EERR!C25`
- **Fila 122**: `4120500000  INTERESES GANADOS` — la referencian: `EERR!C25`

**Qué hay que decidir:** Las dos filas tienen el texto idéntico, así que las dos levantan el mismo importe y se cuenta dos veces (además cada una alimenta líneas distintas: EERR!C25 y EERR!C25). Cuál de las dos sobra es una decisión contable.

### 4211100000 — El mismo código lo usan dos cuentas diferentes

- **Fila 132**: `4211100000  REDONDEO` — la referencian: `Anexo II!D74`
- **Fila 133**: `4211100000  SERVICIOS DE LIMPIEZA` — la referencian: `Anexo II!D45`

**Qué hay que decidir:** El código lo usan dos cuentas diferentes ("4211100000  REDONDEO" y "4211100000  SERVICIOS DE LIMPIEZA"). Borrar cualquiera elimina una cuenta real del balance.

### 4211200000 — El mismo código lo usan dos cuentas diferentes

- **Fila 129**: `4211200000  GASTOS EN EQ. TELEFÓNICOS` — la referencian: `Anexo II!E17`
- **Fila 134**: `4211200000  GASTOS TELEFÓNICOS` — la referencian: `Anexo II!D42`

**Qué hay que decidir:** El código lo usan dos cuentas diferentes ("4211200000  GASTOS EN EQ. TELEFÓNICOS" y "4211200000  GASTOS TELEFÓNICOS"). Borrar cualquiera elimina una cuenta real del balance.

### 4211700000 — El mismo código lo usan dos cuentas diferentes

- **Fila 136**: `4211700000  GASTOS DE CAPACITACION` — la referencian: `Anexo II!D71`
- **Fila 142**: `4211700000  GASTOS CAPACITACIÓN` — la referencian: nadie

**Qué hay que decidir:** El código lo usan dos cuentas diferentes ("4211700000  GASTOS DE CAPACITACION" y "4211700000  GASTOS CAPACITACIÓN"). Borrar cualquiera elimina una cuenta real del balance.

### 4222900000 — Las dos filas son idénticas: el importe se cuenta dos veces

- **Fila 190**: `4222900000  ROPA DE TRABAJO` — la referencian: `Anexo II!E32`
- **Fila 195**: `4222900000  ROPA DE TRABAJO` — la referencian: `Anexo II!E53`

**Qué hay que decidir:** Las dos filas tienen el texto idéntico, así que las dos levantan el mismo importe y se cuenta dos veces (además cada una alimenta líneas distintas: Anexo II!E32 y Anexo II!E53). Cuál de las dos sobra es una decisión contable.

### 4230200000 — El mismo código lo usan dos cuentas diferentes

- **Fila 211**: `4230200000  IMP. A LOS DÉBITOS Y CRÉDITOS LEY 25,413` — la referencian: nadie
- **Fila 212**: `4230200000  IMP. A LOS DEBITOS` — la referencian: `Anexo II!F54`

**Qué hay que decidir:** El código lo usan dos cuentas diferentes ("4230200000  IMP. A LOS DÉBITOS Y CRÉDITOS LEY 25,413" y "4230200000  IMP. A LOS DEBITOS"). Borrar cualquiera elimina una cuenta real del balance.

### 4212800000 — El mismo código lo usan dos cuentas diferentes

- **Fila 152**: `4212800000  TASA AFIP` — la referencian: `Anexo II!D82`
- **Fila 221**: `4212800000  TASAS AFIP` — la referencian: nadie

**Qué hay que decidir:** El código lo usan dos cuentas diferentes ("4212800000  TASA AFIP" y "4212800000  TASAS AFIP"). Borrar cualquiera elimina una cuenta real del balance.

## Balance Acumulado R$ — 4 caso(s)

### 4120500000 — Las dos filas son idénticas: el importe se cuenta dos veces

- **Fila 117**: `4120500000  INTERESES GANADOS` — la referencian: `EERR!C24`
- **Fila 123**: `4120500000  INTERESES GANADOS` — la referencian: `EERR!C22`

**Qué hay que decidir:** Las dos filas tienen el texto idéntico, así que las dos levantan el mismo importe y se cuenta dos veces (además cada una alimenta líneas distintas: EERR!C24 y EERR!C22). Cuál de las dos sobra es una decisión contable.

### 4225000000 — El mismo código lo usan dos cuentas diferentes

- **Fila 188**: `4225000000  CESIÓN DE DERECHOS` — la referencian: `Anexo II!D22`
- **Fila 194**: `4225000000  ADELANTO VIAJE` — la referencian: `Anexo II!E14`

**Qué hay que decidir:** El código lo usan dos cuentas diferentes ("4225000000  CESIÓN DE DERECHOS" y "4225000000  ADELANTO VIAJE"). Borrar cualquiera elimina una cuenta real del balance.

### 4230200000 — El mismo código lo usan dos cuentas diferentes

- **Fila 196**: `4230200000  IMP. A LOS DÉBITOS Y CRÉDITOS LEY 25,413` — la referencian: `Anexo II!F56`
- **Fila 197**: `4230200000  IMP. A LOS DEBITOS` — la referencian: nadie

**Qué hay que decidir:** El código lo usan dos cuentas diferentes ("4230200000  IMP. A LOS DÉBITOS Y CRÉDITOS LEY 25,413" y "4230200000  IMP. A LOS DEBITOS"). Borrar cualquiera elimina una cuenta real del balance.

### 4230400000 — Una de las dos nunca levanta su importe

- **Fila 199**: `4230400000  INTERESES RESARCITORIOS` — la referencian: `Anexo II!E58`
- **Fila 208**: `4230400000 INTERESES RESARCITORIOS` — la referencian: `Anexo II!F58`

**Qué hay que decidir:** La fila 208 nunca levanta su importe (su texto no matchea) pero Anexo II!F58 la referencia: esa línea viene mostrando cero. Hay que repuntarla a la fila 199 antes de borrar nada.
